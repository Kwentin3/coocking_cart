# Same-domain MVP entry path. Use an absolute http(s) URL only for a separate contour.
NEXT_PUBLIC_MVP_ENTRY_URL=/mvp

# Show compact asset/placeholders IDs such as A01, A02 over landing visuals.
NEXT_PUBLIC_LANDING_ASSET_DEBUG_IDS=false
