# Decision log

- Дата старта: 2026-05-20
- Статус: активный журнал продуктовых решений

## Принятые решения

| ID | Дата | Решение | Обоснование | Последствия |
| --- | --- | --- | --- | --- |
| D-001 | 2026-05-20 | В первом MVP продукт формирует проект документа, а не юридически финально утвержденную карту. | Безопасность, соответствие требованиям и фактические производственные данные должны подтверждаться предприятием. | В интерфейсе и документах нужен явный статус "проект" и блок предупреждений. |
| D-002 | 2026-05-20 | Для пользователя используется простая формулировка "технологическая карта", но система должна различать ТК и ТТК по характеру блюда. | Пользователь часто мыслит общим термином, а продукт должен помогать выбрать корректный тип документа. | В workflow нужен шаг определения: стандартное блюдо, новое блюдо или адаптация. |
| D-003 | 2026-05-20 | Для авторских и адаптированных блюд по умолчанию предполагается ТТК. | ТТК лучше подходит для новой и нетрадиционной продукции, где важны требования к сырью, безопасности, хранению и пищевой ценности. | В PRD и workflow авторские блюда ведутся к проекту ТТК, если пользователь не подтвердил обратное. |
| D-004 | 2026-05-20 | Первый MVP не делает прямой импорт в iiko, r_keeper, 1С или StoreHouse, а формирует нейтральный структурированный JSON для будущих интеграций. | Прямые интеграции требуют маппинга номенклатуры, единиц измерения, прав и правил учета конкретной системы. | Интеграционные адаптеры остаются следующим этапом после проверки продуктовой ценности. |
| D-005 | 2026-05-20 | Фокус PRD - продуктовая ценность и workflow, без инженерной архитектуры. | Документ должен быть понятен владельцу кафе, технологу, повару, инвестору или партнеру. | PRD не описывает внутренние механизмы реализации, инфраструктуру и архитектурные детали. |
| D-006 | 2026-05-20 | PRD v0.2 остается продуктовым документом без описания инженерной архитектуры. | Документ нужен для обсуждения ценности, сценария и границ ответственности с нетехническими участниками. | В v0.2 усилены workflow, аудитория, ограничения и статусы данных, но не добавлены детали реализации. |
| D-007 | 2026-05-20 | MVP учитывает доступность, редкость и дороговизну ингредиентов, но не считает полноценную себестоимость. | Пользователь может хотеть более доступные ингредиенты, но закупочные цены, поставщики и складской учет требуют отдельного контура. | В PRD себестоимость вынесена из MVP, а замены ингредиентов оставлены как продуктовая помощь. |
| D-008 | 2026-05-20 | В документе должны отображаться статусы данных. | Пользователь должен понимать, какие значения подтверждены, рассчитаны, взяты из справочника или требуют проверки. | В PRD и workflow v0.2 добавлен блок статусов данных. |
| D-009 | 2026-05-20 | Производственная проработка выносится в vNext. | Реальные потери, выход, температура, время и упаковка проверяются фактическим приготовлением, а не только диалогом. | В PRD v0.2 добавлено направление развития после MVP. |
| D-010 | 2026-05-20 | Продукт формирует проект документа, а не лабораторно или юридически подтвержденную карту. | Лабораторные исследования, сроки годности, показатели безопасности и утверждение находятся в зоне ответственности предприятия. | В PRD v0.2 усилена граница ответственности и список неподтверждаемых системой данных. |
| D-011 | 2026-05-20 | В PRD нужен короткий executive summary для первичного обсуждения продукта. | Полный PRD полезен для детализации, но партнерам и знакомым сначала нужен быстрый обзор идеи, ценности, MVP и ограничений. | В начало PRD добавлен раздел "Коротко для обсуждения". |
| D-012 | 2026-05-20 | Термин "canonical JSON" не используется в основном продуктовом PRD как обязательный термин. | Для нетехнических читателей понятнее формулировка "структурированный JSON для будущих интеграций". | В PRD и workflow продуктовый текст смягчен; техническое уточнение можно оставить для будущего blueprint. |
| D-013 | 2026-05-20 | После MVP стоит рассмотреть отдельный режим технолога. | Технологу нужен не только финальный текст, а список спорных полей, фильтры по статусам, возможность подтверждать значения и возвращать проект на доработку. | В PRD и workflow добавлено направление "режим технолога" после MVP. |
| D-014 | 2026-05-20 | Выходной результат должен показываться как набор рабочих блоков, а не как один сплошной текст. | Пользователь должен быстро увидеть документ, таблицу ингредиентов, предупреждения, статусы данных и JSON. | В PRD и workflow добавлен пример выходного результата со статусами данных. |
| D-015 | 2026-05-24 | В репозитории будет два документационных контура: Demo MVP и future production architecture. | Быстрый демонстрационный прототип и будущая production-система имеют разные цели, риски и глубину проектирования. | Документация должна разделять `docs/mvp/` и `docs/production/`, используя общие product docs как baseline. |
| D-016 | 2026-05-24 | Для Demo MVP принимается file-driven context architecture. | Для демонстрации важнее быстро управлять контекстом и правилами без внедрения полноценного semantic context engine. | MVP-документы должны описать сборку context pack из файлов и отделить этот подход от production architecture. |
| D-017 | 2026-05-24 | Статические части контекстного окна управляются markdown-файлами, а не хардкодом в коде. | Роль ассистента, правила продукта, workflow, статусы данных и demo limitations должны редактироваться как документационные артефакты. | Нужно создать `docs/mvp/context/` с управляемыми markdown context layers и manifest. |
| D-018 | 2026-05-24 | Динамическая история диалога хранится в SQLite, а не в markdown-файлах. | История user/assistant сообщений является runtime state, а не документацией или статическим prompt asset. | MVP должен разделять static markdown context и dynamic dialogue history. |
| D-019 | 2026-05-24 | Каждый такт MVP отправляет в LLM markdown context pack и короткую цепочку user/assistant сообщений. | Для демонстрации достаточно отправлять 5-10 последних сообщений целиком, не строя сложную memory/state architecture. | Контракт контекстного окна MVP должен явно описать состав prompt context и ограничение длины истории. |
| D-020 | 2026-05-24 | Structured output используется уже в MVP, но без production-сложности. | Даже демо должно возвращать машинно-разбираемый результат, warnings и статусы данных, но не обязано иметь полную production-схему. | Нужен отдельный `MVP_STRUCTURED_OUTPUT_CONTRACT_v0.1.md` с минимальным контрактом ответа. |
| D-021 | 2026-05-24 | Production state machine и semantic context layer остаются future track. | Их детальная проработка сейчас замедлит демонстрационный MVP и смешает быстрый прототип с production-проектированием. | Production-документы создаются отдельно и не являются блокером для MVP context architecture. |
| D-022 | 2026-05-24 | LLM не должна автоматически создавать и подключать новые context layers в Demo MVP. | Авторасширение слоев размоет контроль над prompt/context assets и может незаметно изменить поведение демо. | Изменение `docs/mvp/context/` выполняется как осознанное документационное изменение, а не runtime-действие модели. |
| D-023 | 2026-05-24 | Перед реализацией Demo MVP создается implementation handoff. | Реализатору нужен короткий порядок чтения, список MVP-задач, инварианты и запреты до выбора стека. | Добавлен `docs/mvp/MVP_IMPLEMENTATION_HANDOFF_v0.1.md`; следующая реализационная задача должна начинаться с него. |
| D-024 | 2026-05-24 | Demo MVP проверяется минимум на двух сценариях: адаптированное блюдо и простое стандартное блюдо. | Один красивый авторский сценарий не проверяет, что ассистент не усложняет простые блюда. | Добавлен `docs/mvp/MVP_DEMO_SCENARIOS_v0.1.md` со сценариями "курица по-вьетнамски" и "яичница/омлет". |
| D-025 | 2026-05-24 | Structured output и structured JSON разделяются терминологически. | Runtime получает полный structured output, а structured JSON является только одним блоком внутри него. | MVP-документы должны не смешивать user answer, document draft и structured JSON. |
| D-026 | 2026-05-24 | Acceptance criteria фиксируются до начала реализации Demo MVP. | Иначе прототип будет оцениваться субъективно и может незаметно уйти в production-усложнение. | Добавлен `docs/mvp/MVP_ACCEPTANCE_CRITERIA_v0.1.md` с критериями документационной и демонстрационной готовности. |
| D-027 | 2026-05-24 | `docs/out/` является технической обменной папкой и не участвует в документационном контуре проекта. | `docs/out/` нужен только для локальной передачи файлов и не должен влиять на каноническую структуру документации. | Папка игнорируется через `.gitignore`; документы должны храниться в канонических разделах `docs/`. |
| D-028 | 2026-05-24 | MVP должен иметь контракт prompt/context layer. | Markdown layers должны быть управляемыми артефактами с ролью, порядком, источником и статусом, а не случайными prompt-фрагментами. | Добавлен `docs/mvp/MVP_PROMPT_LAYER_CONTRACT_v0.1.md`; `context_manifest.yml` расширен metadata слоев. |
| D-029 | 2026-05-24 | Один шаг общения фиксируется как turn contract. | Реализации нужен воспроизводимый цикл: user message, context assembly, LLM call, structured output, сохранение результата и переход к следующему такту. | Добавлен `docs/mvp/MVP_TURN_CONTRACT_v0.1.md`; future state machine остается вне MVP. |
| D-030 | 2026-05-24 | Context Inspector признается важным debug/demo-инструментом. | Для доверия и диагностики нужно видеть, какие layers, история и instructions попали в context window. | Добавлен `docs/mvp/MVP_CONTEXT_INSPECTOR_v0.1.md`; в PRD добавлено только продуктовое упоминание демонстрационной прозрачности. |
| D-031 | 2026-05-24 | Минимальный context trace нужен уже в MVP. | Trace помогает отладить сборку context window и связать ответ LLM с manifest, layers, history и provider/model. | Добавлен `docs/mvp/MVP_CONTEXT_TRACE_v0.1.md`; trace не считается production audit/event log. |
| D-032 | 2026-05-24 | LLM provider integration должна быть изолирована через adapter boundary. | Даже один provider в MVP не должен проникать глубоко в доменную логику и context assembly. | Добавлен `docs/mvp/MVP_LLM_PROVIDER_ADAPTER_NOTE_v0.1.md`; runtime вызывает LLM через минимальный adapter boundary. |
| D-033 | 2026-05-24 | Provider factory и capability matrix остаются future track. | Для Demo MVP достаточно одного provider и простого adapter; multi-provider слой преждевременен. | Production README расширен future direction: provider adapters/factory и capability matrix. |
| D-034 | 2026-05-24 | Контекстное окно должно быть воспроизводимым assembled artifact. | Без воспроизводимости невозможно объяснять ответы, диагностировать prompt drift и строить будущую observability. | `MVP_CONTEXT_WINDOW_CONTRACT_v0.1.md` требует manifest version, layer list, history, last user message, instruction и assembled context preview. |
| D-035 | 2026-05-24 | Product PRD не должен превращаться в инженерный blueprint. | PRD нужен для продуктовой ценности и доверия, а engineering details должны жить в `docs/mvp/` и `docs/production/`. | PRD обновлен только коротким упоминанием debug/inspector view; подробности вынесены в MVP engineering docs. |
| D-036 | 2026-05-24 | В Demo MVP используются только роли `user` и `admin`. | Двух ролей достаточно для чата, demo sessions, Context Inspector и администрирования демо без сложной IAM-модели. | Добавлен `docs/mvp/MVP_ROLES_AND_ACCESS_v0.1.md`; manager/technologist/owner остаются future roles. |
| D-037 | 2026-05-24 | Переменное окружение проектируется через минимальный environment contract. | API keys, provider/model, context paths, database path и debug flags не должны хардкодиться. | Добавлен `docs/mvp/MVP_ENVIRONMENT_CONTRACT_v0.1.md` с placeholders и без реальных значений. |
| D-038 | 2026-05-24 | Bootstrap admin и стартовые настройки задаются через env/bootstrap contract. | Первый запуск должен быть воспроизводимым без ручного изменения кода и без коммита секретов. | Добавлен `docs/mvp/MVP_BOOTSTRAP_DATA_CONTRACT_v0.1.md`. |
| D-039 | 2026-05-24 | Секреты, API-ключи и private deployment credentials не хранятся в репозитории. | Репозиторий должен оставаться безопасным и переносимым между окружениями. | Реальные `.env`, passwords, API keys, tokens и SSH private keys запрещены к коммиту; non-secret deployment context можно фиксировать отдельно. |
| D-040 | 2026-05-24 | Реальная инфраструктурная документация будет заполнена отдельным заданием после передачи IP/domain/SSH/Traefik context. | Сейчас нельзя надежно проектировать deployment без аудита существующего сервера и reverse proxy. | До передачи контекста `docs/ops/` содержит templates/checklists; после передачи non-secret deployment context фиксируется отдельными ops-документами. |
| D-041 | 2026-05-24 | `docs/ops` содержит placeholder-документы до реального серверного аудита. | Нужно заранее зафиксировать, какие данные собрать и какие действия запрещены до read-only аудита. | Добавлены `docs/ops/README.md`, deployment template, server audit checklist и environment notes. |
| D-042 | 2026-05-24 | Demo MVP target domain: `coocking-cart.speechbattle.com`. | Домен передан владельцем проекта как deployment context для будущего демо. | Домен фиксируется в `docs/ops/`, но DNS/TLS/deploy changes не выполняются в этом проходе. |
| D-043 | 2026-05-24 | Deployment target server: `91.132.48.224`. | Сервер передан владельцем проекта как будущая цель деплоя Demo MVP. | IP фиксируется в deployment context; сервер не меняется без отдельного deployment task. |
| D-044 | 2026-05-24 | SSH access is key-based as `root`, but private keys are never stored in repository. | Доступ нужен для read-only аудита и будущего деплоя, но credentials не должны попадать в публичный Git. | В документации можно фиксировать `root@91.132.48.224`; private keys, passwords и tokens запрещены к коммиту. |
| D-045 | 2026-05-24 | На сервере уже есть Docker и Traefik; первый инфраструктурный pass должен быть read-only. | На сервере уже работают контейнеры, и любое изменение может нарушить существующие сервисы. | Создан read-only server audit report; изменения Traefik, Docker networks и containers запрещены без отдельного review. |
| D-046 | 2026-05-24 | Future deployment must not disrupt existing containers or Traefik. | Demo MVP должен встраиваться в существующую инфраструктуру аккуратно, без остановки чужих сервисов. | Будущий deployment task должен сначала согласовать network, labels, path, `.env`, dry-run и rollback. |
| D-047 | 2026-05-24 | Real secrets and `.env` values are kept outside Git. | Репозиторий публичный, поэтому API keys, session secrets, admin credentials и SSH keys нельзя коммитить. | Environment docs допускают только placeholders и non-secret deployment context. |
| D-048 | 2026-05-24 | Перед реализацией MVP нужен frontend visual contract. | Без UI-контракта реализация может стать случайным чатом без понятного результата, warnings, statuses и JSON. | Добавлен `docs/mvp/MVP_FRONTEND_VISUAL_CONTRACT_v0.1.md`. |
| D-049 | 2026-05-24 | User-facing UI language for MVP is Russian. | Основная аудитория MVP и демо-сценарии описаны на русском языке. | Кнопки, предупреждения, empty/error states и основные подписи должны быть на русском; debug field names могут оставаться на английском. |
| D-050 | 2026-05-24 | MVP uses light theme by default; dark/theme customization is future. | Для демо важнее читаемость и скорость реализации, чем theme system. | Базовая светлая тема обязательна; dark theme и theme customizer не входят в MVP. |
| D-051 | 2026-05-24 | UI should prefer icon-first contextual hints over long text legends. | Длинные легенды перегружают демо-интерфейс и мешают видеть рабочий документ. | Для пояснений используются compact icons с popover/sheet/modal и accessibility label/title. |
| D-052 | 2026-05-24 | Critical warnings cannot be hidden only behind icons. | Критичные риски по срокам, безопасности, неподтвержденным данным и проектному статусу должны быть видимы пользователю. | Critical warnings показываются текстом в отдельном warning block, icon может быть только дополнением. |
| D-053 | 2026-05-24 | Context Inspector is admin/debug UI. | Prompt layers, context trace и assembled context относятся к диагностике и не должны показываться обычному user. | `user` видит результат своей сессии; `admin` видит Context Inspector без secrets/API keys. |
| D-054 | 2026-05-24 | MVP target LLM provider family is Gemini, exact model via env. | Для первой реализации выбран практичный provider family, но точный model id может меняться. | `LLM_PROVIDER=gemini` ожидается для MVP; `LLM_MODEL=<GEMINI_FLASH_MODEL_ID>` задается через environment. |
| D-055 | 2026-05-24 | LLM adapter boundary remains mandatory regardless of provider. | Даже при одном provider нельзя размазывать SDK/model-specific детали по доменной логике. | Runtime вызывает LLM через provider adapter; provider factory/capability matrix остаются future track. |
| D-056 | 2026-05-24 | Перед реализацией Demo MVP нужен execution roadmap. | Документации стало достаточно много, и без порядка агент может начать не с той фазы или смешать MVP с production. | Добавлен `docs/mvp/MVP_IMPLEMENTATION_ROADMAP_v0.1.md`. |
| D-057 | 2026-05-24 | Реализация должна идти фазами с gate-критериями. | Фазовый порядок снижает риск преждевременного UI, deployment, production state machine или hardcoded prompts. | Roadmap задает Phase 0-9, acceptance/gate для каждой фазы и запреты на фазах. |
| D-058 | 2026-05-24 | Агент не должен переходить к следующей фазе без выполнения acceptance текущей фазы. | Иначе ошибки context loader, storage, adapter или structured output будут маскироваться последующим UI. | Handoff и acceptance criteria ссылаются на roadmap gates. |
| D-059 | 2026-05-24 | Deployment остается отдельной фазой/задачей и не выполняется в рамках реализации без отдельного подтверждения. | На сервере уже есть Docker, Traefik и рабочие containers; любые изменения требуют отдельного deployment task. | Roadmap Phase 9 ограничен подготовкой, без server changes. |
| D-060 | 2026-05-24 | Stack choice должен быть зафиксирован до или в Phase 1. | Стек влияет на структуру проекта, runtime, UI и deployment preparation. | Если стек не задан владельцем, агент предлагает минимальный стек и обосновывает выбор before coding. |
| D-061 | 2026-05-24 | В репозитории допускается `.env.example` только с безопасными non-secret values и placeholders. | Реализатору нужен стартовый env pack, но публичный репозиторий не должен содержать secrets. | Добавлен root `.env.example`; реальные `.env` файлы остаются вне Git. |
| D-062 | 2026-05-24 | Реальные `.env`, API keys, session secrets и bootstrap credentials не коммитятся. | LLM key, session secret и admin credentials являются чувствительными данными. | `.gitignore` защищает `.env`, `.env.*` и runtime SQLite-файлы; secrets передаются вне Git. |
| D-063 | 2026-05-24 | Для Demo MVP target LLM provider value: `LLM_PROVIDER=gemini`. | Gemini выбран как целевое provider family для первой реализации. | Provider задается через env/config и вызывается только через adapter boundary. |
| D-064 | 2026-05-24 | Exact Gemini model id задается через `LLM_MODEL` и подтверждается на момент реализации. | Имена моделей могут меняться, поэтому документация не должна фиксировать model id как константу. | `.env.example` содержит `LLM_MODEL=<GEMINI_FLASH_MODEL_ID>`; агент реализации проверяет актуальное имя модели перед запуском. |
| D-065 | 2026-05-24 | Deployment non-secret values могут быть отражены в `.env.example`. | Домен, IP и deploy user уже переданы как deployment context и не являются secrets. | `.env.example` содержит `APP_BASE_URL`, `PUBLIC_DOMAIN`, `DEPLOY_HOST` и `DEPLOY_USER`; Traefik values остаются placeholders. |
| D-066 | 2026-05-24 | Runtime SQLite-файлы не коммитятся. | SQLite содержит runtime dialogue state и служебные результаты тактов, а не проектную документацию. | `.gitignore` исключает `*.sqlite`, `*.sqlite3`, `data/*.sqlite` и `data/*.sqlite3`. |
| D-067 | 2026-05-24 | Любое значение вида `<...>` в `.env.example` считается placeholder и not configured. | Placeholder не должен случайно пройти как реальный API key, model id, admin credential или Traefik value. | Реализация должна валидировать placeholders и показывать user-safe/admin-debug ошибку конфигурации для обязательных значений. |
| D-068 | 2026-05-24 | `root@91.132.48.224` фиксируется только как текущий demo deployment context. | Root-доступ был передан для read-only audit и будущей подготовки демо, но это не production hardening pattern. | Private SSH key не хранится в Git; dedicated deploy user остается future ops hardening и не блокирует Demo MVP. |
| D-069 | 2026-05-24 | Перед реализацией агент должен стартовать с финальной консолидированной документационной ветки, а не с default branch, если default устарел. | Default branch может не содержать roadmap, env-pack, visual contract и ops/context updates. | Для реализации использовать `docs/mvp-final-docs-before-implementation` или cleanup-ветку после merge; позже можно обновить default branch отдельно. |
| D-070 | 2026-05-24 | Environment, bootstrap, LLM docs и `.env.example` являются mandatory Phase 0 intake documents. | Реализация без этих документов рискует захардкодить secrets, принять placeholders как реальные значения или вызвать LLM неправильно. | Roadmap Phase 0 и handoff помечают env/bootstrap/LLM docs как обязательные до старта реализации. |
| D-071 | 2026-05-24 | Placeholder validation является gate перед любым реальным LLM call. | Placeholder model id или API key не должен попасть в provider adapter как рабочая конфигурация. | Перед LLM call required env values с `<...>` должны давать user-safe/admin-debug ошибку конфигурации. |
| D-072 | 2026-05-24 | Gemini Structured Output в MVP задается provider-level schema в generation config, а не prompt-only форматированием. | Официальная спецификация Gemini Structured Outputs описывает JSON MIME type и JSON Schema/response schema в generation config; prompt должен объяснять задачу, но не заменять schema mechanism. | Context window содержит task/domain instruction, а Gemini adapter передает `responseFormat`/JSON schema отдельно; trace/Context Inspector должны показывать schema/config как отдельную часть такта. |
| D-073 | 2026-05-24 | Admin user CRUD входит в Demo MVP как demo operations, но не как production IAM. | Для демо нужен способ заводить пользователей без ручного SQLite/bootstrap вмешательства, но сложная RBAC/ABAC-модель преждевременна. | В CRUD остаются только роли `user`/`admin`; password hashes и secrets не возвращаются; текущий/последний admin защищен от удаления или понижения. |
| D-074 | 2026-05-24 | Demo chat sessions получают минимальный CRUD через icon-first controls. | Пользователю нужно управлять рабочими чатами без ручного доступа к SQLite, а интерфейс уже принят как icon-first. | `user` управляет своими sessions; `admin` управляет видимыми sessions для отладки; delete требует подтверждения и остается demo storage behavior. |

## Как обновлять журнал

Новые решения добавляются отдельной строкой с датой, коротким обоснованием и последствиями. Если решение отменено или заменено, его не нужно удалять: добавляется новая запись со ссылкой на предыдущее решение.
